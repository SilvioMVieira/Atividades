1. Algoritmo média aritmética de quatro notas.

    variaveis
        nomeAluno = "Silvio Vieira"
        nota1 = 7 //nota da primeira unidade
        nota2 = 6 //nota da segunda unidade
        nota3 = 8 //nota da terceira unidade
        nota4 = 5 //nota da quarta unidade
        mediaFinal = 7 //media para aprovação
        mediaAluno = (nota1 + nota2 + nota3 + nota4)/4

        if (mediaAluno >= mediaFinal){
            alert(nomeAluno + " foi Aprovado!")
        }
        else{
            alert(nomeAluno + " foi Reprovado!")
        }

2. algoritmo "Login"
var
   usuario: caracter
   senha: caracter
inicio
      escreva ("Informe seu usuário: ")
      leia (usuario)
      escreva ("Informe sua senha: ")
      leia (senha)
      se senha="ASDFG" então
      escreva ("Acesso Permitido")
      se não
      escreva("Acesso Negado")
      fimalgoritmo

3.  var L1=Number(prompt("Digite o valor do lado1:"))
    var L2=Number(prompt("Digite o valor do lado2:"))
    var L3=Number(prompt("Digite o valor do lado3:"))

    if(L1<L2+L3 && L2<L1+L3 && L3<L1+L2){
        console.log("Temos um triângulo")
    if (L1==L2 && L1==L3 && L2==L3){
        console.log("Triângulo Equilátero")
    }else if(L1!=L2 && L3!=L1 && L2!=L3) {
        console.log("Triângulo escaleno")
    }else {
        console.log("Triângulo isósceles")
    }
    }else{
        console.log("Não temos um triângulo!")
    } 
    

4.     
